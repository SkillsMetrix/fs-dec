package com.model;


public sealed interface Payment permits UpiPayment,CardPayment,CashPayment{
	public String paymentId();
	public String userId();
	public double amount();
}


---------

package com.model;

public final record UpiPayment (
		
		String paymentId,
		String userId,
		double amount,
		String upiId
		
		) implements Payment{}



package com.model;

public final record CashPayment(
		String paymentId,
		String userId,
		double amount
	)implements Payment{}




package com.model;

public final record CardPayment(
		String paymentId,
		String userId,
		double amount,
		String cardNumber
		
		
		)implements Payment{}
